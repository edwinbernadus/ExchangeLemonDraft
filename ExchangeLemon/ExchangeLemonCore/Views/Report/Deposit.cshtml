@model IEnumerable<BlueLight.Main.MvInquiryBalance>

@{
    ViewBag.Title = "Index";
}

<h2 class="color-blue-one">Deposit / Withdrawal</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.CurrencyCode)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.AvailableBalance)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HoldBalance)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Balance)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td class="clear_row">
            @Html.DisplayFor(modelItem => item.CurrencyCode)
        </td>
        <td class="clear_row">
            @Html.DisplayFor(modelItem => item.AvailableBalance)
        </td>
        <td class="clear_row">
            @Html.DisplayFor(modelItem => item.HoldBalance)
        </td>
        <td class="clear_row">
            @Html.DisplayFor(modelItem => item.Balance)
        </td>

        <td class="clear_row">
            @* @Html.ActionLink("Deposit", "Deposit", "Bitcoin", new {id = item.CurrencyCode},"") | *@
            @* @Html.ActionLink("Withdrawal", "Withdrawal", "Bitcoin", new {id = item.CurrencyCode},"") | *@
            @Html.ActionLink("Insert Manual", "InsertManual", "Bitcoin", new { id = item.CurrencyCode }, "")
            @*@Html.ActionLink("Deposit", "Deposit", new { id=item.Id }) |
            @Html.ActionLink("Withdrawal", "Withdrawal", new { id=item.Id })*@

            @*@Html.ActionLink("Details", "Details", new { id = item.Id })*@
        </td>
    </tr>
}

</table>
