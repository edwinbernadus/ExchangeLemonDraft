@model IEnumerable<BlueLight.Main.MvDetailReportOrder>

@{
    ViewBag.Title = "Order History Detail";
}

<h2>Order History Detail</h2>

<p>
    @*@Html.ActionLink("Create New", "Create")*@
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Rate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Rate)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Amount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.LeftAmount)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsBuy)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.IsComplete)
        </th>

        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @BlueLight.Main.DisplayHelper.RateDisplay(item.Rate)
            @*@item.Rate.ToString("0000000")*@
            @*@Html.DisplayFor(modelItem => item.Rate)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Rate)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Amount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.LeftAmount)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsBuy)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.IsComplete)
        </td>

        <td>
            @*@Html.ActionLink("Cancel", "Cancel", new { id = item.Id })*@

        </td>
    </tr>
}

</table>
